#ifndef UTILS_H
#define UTILS_H

#include <stdbool.h>
#include <windows.h>

#define MS 50
#define TITLE "Elarion"

void write(char string[]);
void introduceWorld(void);
void rollDiceSix(void);
void drawDiceSix(int number);

#endif
